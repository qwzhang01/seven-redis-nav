# ============================================================
# 量化交易系统 — 生产环境 Docker Compose
# 说明：只部署应用服务，接入已有基础设施（Redis/PostgreSQL/Kafka/MinIO）
# 使用：APP_VERSION=1.0.0 docker compose -f docker-compose.prod.yml up -d
# ============================================================

services:
  quant_app:
    image: quant_app:${APP_VERSION:-latest}
    build:
      context: ..
      dockerfile: publish/Dockerfile.prod
    container_name: quant_app
    restart: unless-stopped
    env_file:
      - .env.prod
    ports:
      # 仅绑定本地回环地址，由 Nginx(quantmeta) 反向代理对外暴露
      - "127.0.0.1:8000:8000"
    volumes:
      # 日志持久化到宿主机
      - ../logs:/app/logs
    networks:
      - prod_network
    healthcheck:
      test: ["CMD", "curl", "-f", "-s", "-X", "GET", "http://localhost:8000/api/v1/m/health/", "-H", "accept: application/json"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "10"
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

# ============================================================
# 网络配置：加入已有生产网络（由环境变量 PROD_NETWORK 指定，默认 script_default）
# 该网络由基础设施管理，quant_app 加入后可与 Redis/PostgreSQL/Kafka/MinIO/quantmeta 互通
# 启动方式：PROD_NETWORK=script_default APP_VERSION=1.0.0 docker compose -f docker-compose.prod.yml up -d
# ============================================================
networks:
  prod_network:
    external: true
    name: ${PROD_NETWORK:-script_default}
